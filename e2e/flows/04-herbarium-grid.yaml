appId: com.pixelherbarium.app
---
# Navigate to Home tab and verify it is visible (CHECKIN_MODE: home tab shows bloom feed)
- runFlow: ./02-login-email.yaml
- tapOn:
    id: "tab.home"
- assertVisible:
    id: "home.container"
