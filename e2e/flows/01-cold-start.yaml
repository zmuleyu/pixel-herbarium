appId: com.pixelherbarium.app
---
# Verify app launches and shows onboarding on first cold start (splash completes, no crash)
- launchApp:
    clearState: true
- extendedWaitUntil:
    visible:
      id: "onboarding.skip"
    timeout: 15000
