+-----------------------------------------------------------------------+
| **「桌宠」语音功能技术方案**                                          |
|                                                                       |
| Desktop Pet Voice Feature --- Technical Proposal                      |
+-----------------------------------------------------------------------+

*基于 NoizAI 开源 Skill 及 Kokoro 本地引擎的语音集成方案*

  ---------------------- ------------------------------------------------
  **文档版本**           V1.0

  **编制日期**           2026-03-04

  **文档状态**           Draft

  **参考来源**           NoizAI/skills 开源项目
  ---------------------- ------------------------------------------------

1 项目背景与目标

当前的桌宠应用仅通过视觉动画和文字气泡与用户交互，缺乏音频维度的表达。参考
NoizAI 开源项目在 OpenClaw
上的成功实践------"一旦拥有声音，在心理层面感受到了它的存在"------本方案旨在为桌宠接入完整的语音能力，使其从"可视工具"进化为"可听伙伴"。

1.1 核心目标

-   为桌宠角色赋予专属音色，建立"声音即人设"的用户认知

-   支持本地离线 + 云端双模式 TTS，兼顾隐私与功能

-   实现声音克隆，用户可自定义桌宠音色

-   支持情感化语音输出，让桌宠交互更有温度

-   多角色多音色，听声音就知道谁在说话

2 整体架构

系统采用四层架构设计，各层职责清晰、解耦良好：

  --------------- --------------------- ---------------------------------------
  **层级**        **组件**              **职责说明**

  交互表现层      AudioPlayer + LipSync 音频播放、口型动画同步、音量/语速控制

  调度逻辑层      VoiceRouter           语音触发判断、场景路由、情绪参数注入

  音色管理层      VoiceProfileManager   音色库管理、克隆音色存储、角色绑定

  语音引擎层      Kokoro / Noiz API     TTS 合成、声音克隆、情感化语音生成
  --------------- --------------------- ---------------------------------------

数据流路径：LLM 输出文本 → VoiceRouter 判断是否需要语音 →
VoiceProfileManager 获取当前角色音色 → TTS 引擎合成音频 → AudioPlayer
播放并驱动口型动画。

3 TTS 引擎选型

参考 NoizAI 开源项目的双模式设计，桌宠同样采用"本地 + 云端"双引擎架构。

3.1 方案对比

  -------------- ---------------------------- --------------------------------
  **维度**       **Kokoro 本地引擎**          **Noiz 云端 API**

  **运行环境**   纯本地，无网络依赖           需联网，调用云端 API

  **模型大小**   约 300MB，需本地下载         无本地模型，轻量客户端

  **声音克隆**   ✘ 不支持                     ✔ 支持，丢入参考音频即可

  **情感化**     基础语调控制                 语气词 / 情绪参数 / 场景预设

  **响应延迟**   约 200--500ms                约 500--1200ms（含网络）

  **隐私安全**   数据不出本地                 文本传输至云端

  **费用**       免费 / 纯算力成本            有免费额度，超出按量计费

  **适用场景**   日常闲聊、提醒、简单交互     角色扮演、个性音色、高情感场景
  -------------- ---------------------------- --------------------------------

+-----------------------------------------------------------------------+
| **✅ 推荐策略**                                                       |
|                                                                       |
| 默认使用 Kokoro                                                       |
| 本地引                                                                |
| 擎处理日常语音，当用户触发声音克隆、情感化语音等高级功能时自动切换至  |
| Noiz 云端。落地时可同时评估 GPT-SoVITS、Fish Speech                   |
| 等开源方案作为替补。                                                  |
+-----------------------------------------------------------------------+

3.2 引擎抽象接口

为保证引擎可替换，定义统一的 ITTSEngine 接口：

> interface ITTSEngine {
>
> synthesize(text: string, voiceId: string, emotion?: EmotionParams):
> Promise\<AudioBuffer\>;
>
> cloneVoice(referenceAudio: Blob): Promise\<string\>; // 返回 voiceId
>
> listVoices(): Promise\<VoiceInfo\[\]\>;
>
> getEngineType(): \'local\' \| \'cloud\';
>
> }

两个引擎分别实现该接口，VoiceRouter 通过策略模式决定调用哪个实例。

4 核心模块设计

4.1 VoiceRouter 语音调度器

语音调度器是整个语音系统的"大脑"，负责决定什么时候说、用什么声音说、带什么情绪说。

**触发策略矩阵**

  ---------------- -------------------- ------------------ -----------------
  **触发类型**     **场景示例**         **优先级**         **引擎选择**

  用户主动指令     "用语音回答我"       P0 最高            按当前配置

  定时提醒         闹钟、日程提醒       P1 高              Kokoro 本地

  情绪互动         开心、撞开心、撤娇   P1 高              Noiz 云端

  系统事件         天气播报、新消息     P2 中              Kokoro 本地

  闲聊对话         日常聊天回复         P2 中              用户可配置

  背景音效         打哈欠、哼歌、叹气   P3 低              本地音效库
  ---------------- -------------------- ------------------ -----------------

**调度流程伪代码**

> function onLLMOutput(text, context) {
>
> const shouldSpeak = VoiceRouter.evaluate(context);
>
> if (!shouldSpeak) return renderTextBubble(text);
>
> const profile = VoiceProfileManager.getProfile(context.roleId);
>
> const emotion = EmotionAnalyzer.detect(text, context);
>
> const engine = EngineSelector.pick(profile, emotion);
>
> const audio = await engine.synthesize(text, profile.voiceId, emotion);
>
> AudioPlayer.play(audio);
>
> LipSync.drive(audio, petAvatar);
>
> }

4.2 VoiceProfileManager 音色管理器

音色管理器负责音色的全生命周期：发现、克隆、存储、绑定和切换。

**数据模型**

> interface VoiceProfile {
>
> id: string; // 唯一标识
>
> name: string; // 显示名称，如"活泼少女"
>
> engineType: \'kokoro\' \| \'noiz\'; // 所属引擎
>
> voiceId: string; // 引擎侧音色 ID
>
> referenceAudioPath?: string; // 克隆源音频路径
>
> emotionPresets: EmotionMap; // 情绪预设参数
>
> boundRoles: string\[\]; // 绑定的桌宠角色 ID
>
> }

**音色获取方式**

-   **预设音色库：**系统内置多种风格音色（温柔女声、成熟男声、可爱童声等），用户在设置页选择即可

-   **声音克隆：**用户上传一段 5--15 秒参考音频，系统调用 Noiz API 的
    clone 接口生成专属 voiceId，并持久化存储

-   **在线搜索克隆：**用户输入目标人物名称，系统自动在线寻找其公开语音并提取干净参考样本进行克隆（对应
    NoizAI 的 Skill ②）

4.3 EmotionAnalyzer 情感分析器

情感分析器根据对话上下文生成语音情感参数，让桌宠说话"带感情"而不是机械读字。

> interface EmotionParams {
>
> emotion: \'happy\' \| \'sad\' \| \'angry\' \| \'surprised\' \|
> \'calm\' \| \'playful\';
>
> intensity: number; // 0.0 \~ 1.0
>
> speed: number; // 0.8 \~ 1.3 语速倍率
>
> pitch: number; // -2.0 \~ 2.0 音调偏移
>
> }

分析策略包括三个维度：文本语义分析（根据关键词和句式判断情绪）、会话上下文（用户连续抱怨时切换为安慰语气）、桌宠人设影响（活泼角色偏向
playful，成熟角色偏向 calm）。

4.4 AudioPlayer 与 LipSync

**AudioPlayer 音频播放器**

-   支持音频格式：opus（首选，压缩比高）、mp3、wav

-   实现播放队列，防止多条语音叠加

-   支持打断机制：用户新输入时可立即停止当前语音

-   音量跟随系统音量，可单独调节

**LipSync 口型同步**

-   基于音频振幅分析驱动桌宠嘴型张合动画

-   轻量级实现：提取音频波形的 RMS 能量値，映射到 3--5 个嘴型帧

-   进阶实现：引入音素识别（viseme），实现唇形与发音的精确匹配

5 安装与集成流程

参考 NoizAI 开源项目的 Skill 安装机制，桌宠的语音功能以"Skill
插件"形式接入，而非硬编码到主程序。

5.1 环境准备

  --------------------- -------------------------------------------------
  **环境项**            **要求**

  **Node.js**           ≥ 18.0，用于 Skill 运行时

  **Python**            ≥ 3.10，Kokoro 本地模型依赖

  **磁盘空间**          Kokoro 模型约 300MB，音色缓存约 50MB

  **Noiz API Key**      免费注册获取，配置在 .env 或桌宠设置页

  **音频权限**          桌面端需获取系统音频播放权限
  --------------------- -------------------------------------------------

5.2 安装步骤

**Step 1 --- 安装 TTS Skill**

> npx skills add NoizAI/skills \--full-depth \--skill tts -y

**Step 2 --- 配置引擎参数**

> \# .env 文件
>
> TTS_ENGINE=kokoro \# 默认本地引擎
>
> NOIZ_API_KEY=your_api_key \# 云端引擎可选
>
> NOIZ_DEFAULT_VOICE=zh-female-warm

**Step 3 --- 初始化音色**

首次启动时引导用户从预设库选择音色，或上传参考音频进行克隆。配置完成后自动绑定到当前桌宠角色。

**Step 4 --- 验证测试**

对桌宠说"用语音和我打个招呼"，确认音频正常播放、口型动画同步、音色符合预期。

6 多角色多音色方案

参考 NoizAI
社区中用户"让不同角色用不同音色回复，听声音就知道谁"的实践，桌宠应支持多角色多音色体系。

6.1 角色---音色绑定模型

> interface PetRole {
>
> roleId: string; // 角色唯一 ID
>
> name: string; // 角色名，如"啤啤""大学士"
>
> personality: string; // 人设描述
>
> voiceProfileId: string; // 绑定的音色 ID
>
> defaultEmotion: string; // 默认情绪基调
>
> }

6.2 应用场景

-   **助理角色：**绑定成熟稳重的女声，默认 calm
    情绪，负责日程提醒、邮件摘要

-   **陷聊角色：**绑定活泼可爱的童声，默认 playful
    情绪，负责闲聊、吐槽、讲笑话

-   **客服角色：**绑定温和专业的男声，默认 calm
    情绪，负责技术问答、查询操作

-   **克隆角色：**用户上传任意音频克隆出独一无二的音色，与自定义人设绑定

7 关键技术挑战与应对

  ------------------- ---------------------------- ------------------------------
  **挑战**            **影响**                     **应对方案**

  **延迟敏感**        语音生成太慢会破坏交互节奏   流式 TTS（边生成边播放）+
                                                   本地引擎优先

  **音频压缩**        未压缩 wav 文件体积大        默认使用 opus 编码，压缩比
                                                   10:1

  **多语言支持**      用户可能中英混杂输入         引擎层自动语种检测，分段合成

  **并发播放**        多条语音叠加导致混乱         播放队列 +
                                                   打断机制，同时只播一条

  **离线可用性**      无网络时云端不可用           自动降级到 Kokoro 本地引擎

  **存储占用**        模型 + 缓存音频占空间        模型懒加载 + 音频缓存 LRU
                                                   淘汰策略
  ------------------- ---------------------------- ------------------------------

8 开发里程碑

  ---------- ---------------- --------------------------- ----------------
  **阶段**   **时间**         **交付物**                  **标志**

  **P0**     Week 1--2        TTS 引擎接入 + 基础播放     可听到桌宠说话

  **P0**     Week 3           音色管理器 + 预设库         可切换音色

  **P1**     Week 4--5        VoiceRouter 调度逻辑        智能触发语音

  **P1**     Week 6           声音克隆功能                自定义音色可用

  **P2**     Week 7--8        情感分析 + LipSync          情感语音 +
                                                          口型同步

  **P2**     Week 9           多角色多音色体系            多角色独立音色

  **P3**     Week 10          性能优化 + 回归测试         正式发布就绪
  ---------- ---------------- --------------------------- ----------------

9 技术选型总结

  ------------------ -------------------------- --------------------------
  **模块**           **选型**                   **备选**

  **本地 TTS**       Kokoro（NoizAI Skill）     Piper / VITS

  **云端 TTS**       Noiz API                   Azure TTS / ElevenLabs

  **声音克隆**       Noiz Clone API             GPT-SoVITS / Fish Speech

  **音频格式**       Opus（默认） / WAV         MP3

  **口型同步**       RMS 能量映射（轻量）       Viseme 音素映射（进阶）

  **桌面端播放**     Web Audio API / Electron   Qt QMediaPlayer

  **Skill 管理**     npx skills CLI             自建插件市场
  ------------------ -------------------------- --------------------------

+-----------------------------------------------------------------------+
| **核心理念**                                                          |
|                                                                       |
| 语音不只是功                                                          |
| 能，而是人格的载体。桌宠加入语音后，从"看得见的工具"变成"听得到的伙伴 |
| "。关键在于让音色与角色绑定、让触发场景自然合理、让情感表达丰富多层。 |
+-----------------------------------------------------------------------+
