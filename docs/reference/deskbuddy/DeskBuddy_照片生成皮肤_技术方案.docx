**DeskBuddy**

**"照片→专属桌宠皮肤"技术方案**

基于 AI 风格化 + Spine Skin 系统的个性化角色生成方案

v0.1 \| 2026.03

1\. 设计哲学与约束条件

1.1 核心原则："换皮不换骨"

整个方案的最高约束是：用户上传的照片只影响角色的"皮肤"（纹理贴图），绝不触碰"骨骼"（骨骼结构、动画序列、状态机转换逻辑）。这意味着：

-   所有预制的 30 组动画对自定义皮肤100%复用，零额外制作成本

-   动画调度器、事件队列、优先级系统全部不受影响

-   失败时静默回退到默认皮肤，不影响任何核心功能

1.2 与现有架构的关系

本方案是 DeskBuddy 四层架构中「表现层」的一个可选扩展模块，位于 Spine
Skin 系统之上。它不修改任何已有层的接口，只增加一条"照片输入 → Skin
资产输出"的新通道：

  ----------------- -------------------------- --------------------------
  **架构层**        **已有职责**               **本方案新增**

  **表现层**        PixiJS + Spine 渲染\       AI 皮肤生成管线\
                    Skin 切换、动画混合        Skin 纹理热替换

  **状态层**        XState v5 并行状态机       无变更

  **服务层**        Tauri v2 Rust Backend\     AI API 调用代理\
                    OAuth2、凭据管理           图片预处理（Rust）

  **接入层**        DataSource trait 插件体系  无变更
  ----------------- -------------------------- --------------------------

1.3 开发优先级定位

本功能位于 Phase 6+（正式角色资产完成后），前置条件是：Spine Skin
系统已跑通手动切换（至少 2 套预制皮肤可用）、Spine
图集模板规格已确定、动画调度器支持 Skin
热切换。不满足这些条件时，不应启动本模块开发。

2\. 整体管线设计

2.1 端到端流程

用户从上传照片到看到专属桌宠，全程分为四个阶段：

  ---------- -------------------- --------------------- ---------------------
  **阶段**   **操作**             **执行位置**          **耗时预估**

  **Stage    照片上传与预处理     前端裁剪 + Rust 压缩  \< 1s（本地）
  1**                                                   

  **Stage    AI 风格化转换        Rust → 云端 AI API    3\~8s（网络）
  2**                                                   

  **Stage    部件拆分与纹理生成   Rust → 云端 AI API    5\~15s（网络）
  3**                                                   

  **Stage    Skin 组装与热替换    前端 PixiJS + Spine   \< 0.5s（本地）
  4**                             RT                    
  ---------- -------------------- --------------------- ---------------------

**总用户感知时间：10\~25
秒**。过程中角色保持当前皮肤正常运行，生成完成后平滑过渡到新皮肤。

2.2 数据流架构图

整体数据流从左到右经过四个处理节点：

\[Photo JPEG/PNG\] ─── 前端裁剪框 ───▶ \[512x512 标准化图片\]

│

└─── Tauri IPC ───▶ Rust Backend

│

┌───────┴───────┐

│ AI API Proxy │ ──▶ 风格化 API ──▶ \[卡通风格全身图\]

│ (reqwest) │ ──▶ 部件拆分 API ──▶ \[分层部件 PNG\]

└───────┬───────┘

│

┌───────┴───────┐

│ Atlas Packer │ ──▶ \[1024x1024 图集 + JSON\]

└───────┬───────┘

│

Tauri IPC ─▶ 前端

│

spine.setSkin(customSkin) ──▶ 桌宠应用新皮肤

3\. 各阶段详细设计

3.1 Stage 1：照片上传与预处理

3.1.1 前端裁剪组件

提供一个内置裁剪框，引导用户将人物主体置中。技术选型为
react-easy-crop（轻量、无额外依赖），输出固定尺寸的裁剪区域。

**输入约束：**JPEG / PNG，最大 5MB，最小分辨率
256x256px。超出限制时前端直接拒绝并提示原因。

**输出规格：**512x512px
PNG，透明背景（或统一白色背景）。这是后续所有处理的标准输入尺寸。

3.1.2 Rust 侧预处理

前端通过 Tauri IPC 将裁剪后的图片以 base64 传递到 Rust Backend。Rust
侧执行：

-   图片解码与格式校验（image crate）

-   尺寸归一化到 512x512（如果前端未处理）

-   可选：人脸检测预检（确保照片包含人物，可用 ort crate
    加载轻量人脸检测 ONNX 模型）

-   输出：标准化的 base64 PNG，就绪发送给 AI API

3.2 Stage 2：AI 风格化转换

3.2.1 目标

将真人照片转化为与桌宠画风一致的卡通风格全身图。关键是保留用户的视觉特征（发型、发色、眼睛形状、配饰、服装颜色），同时统一到目标美术风格。

3.2.2 AI API 选型矩阵

  ---------------- -------------------------------- ------------------------------------ --------------------
  **方案**         **优势**                         **劣势**                             **调用方式**

  **GPT-4o**\      风格控制精准、支持复杂           调用费用较高、依赖 OpenAI 生态       POST
  **image gen**    prompt、全身图生成质量高                                              /v1/images/edits\
                                                                                         model: gpt-image-1\
                                                                                         传入原图 + prompt

  **Gemini**\      多模态原生支持、响应快、成本低   风格稳定性需调试、图片编辑能力较弱   POST
  **2.0 Flash**                                                                          generateContent\
                                                                                         multipart: image +
                                                                                         prompt

  **Stable**\      可自托管、风格 LoRA\             需自建 GPU 服务、部署运维复杂        POST
  **Diffusion**\   可训练、无 API 调用费                                                 /sdapi/v1/img2img\
  **img2img**                                                                            自托管 ComfyUI 或\
                                                                                         云 GPU 服务
  ---------------- -------------------------------- ------------------------------------ --------------------

**推荐策略：**初期用 GPT-4o 或 Gemini Flash
快速验证效果，产品稳定后评估是否迁移到自托管 SD
以降低成本。服务层抽象为统一接口，后端可透明切换。

3.2.3 Prompt 工程

风格化的核心是 prompt 设计。分为两部分：

**系统
prompt（固定）：**定义目标画风、角色比例、线条风格、配色方案。例如："Transform
this photo into a cute chibi-style full-body character. The character
should have a 2.5-head proportion, soft pastel outlines, large
expressive eyes. Keep the person's hair style, hair color, eye color,
clothing colors, and distinctive accessories. The character faces
slightly left at 3/4 angle. White background, full body visible
including feet. Style consistent with Japanese kawaii desktop mascot."

**动态
prompt（变量）：**根据用户选择的角色风格模板注入额外约束。如用户选择"像素风"则追加
pixel art 约束，选择"写实 Q 版"则追加渐变、阴影约束。

3.2.4 输出规格

风格化 API 返回一张 512x512
PNG，内容为卡通风格的全身角色立绘（白色或透明背景）。这张图将作为 Stage
3 的输入。

3.3 Stage 3：部件拆分与纹理生成

3.3.1 核心问题

Spine Skin 的工作原理是：同一套骨骼上的每个 slot（插槽）可以挂载不同
skin 的
attachment（附件）。因此我们需要把风格化后的全身图拆分为与骨骼插槽一一对应的部件图片。

3.3.2 插槽-部件映射规范

基于文档 01 定义的角色结构，标准插槽列表如下：

  --------------- ---------------------- ------------------- -------------------------
  **Slot 名称**   **对应部位**           **纹理尺寸**        **备注**

  **head**        头部（含发型、耳朵）   256x256             最重要的个性化部件

  **face**        表情层（眼、眉、嘴）   128x128             覆盖在 head 上方

  **torso**       躯干（含上装）         192x256             服装颜色"最主要的认同感

  **arm_L /       左右手臂               64x192              含袖子纹理
  arm_R**                                                    

  **hand_L /      左右手掌               64x64               肤色匹配
  hand_R**                                                   

  **leg_L /       左右腿                 80x192              含裤子/裙子纹理
  leg_R**                                                    

  **foot_L /      左右脚                 64x64               鞋子纹理
  foot_R**                                                   

  **accessory**   配饰（可选）           128x128             帽子、眼镜、背包等
  --------------- ---------------------- ------------------- -------------------------

3.3.3 拆分方案：AI 语义分割 + 模板引导

核心思路是"用 AI 做粗活，用模板做精活"：

**第一步，AI 语义分割：**将风格化全身图发送给 AI API，prompt
要求识别并分离各身体部件，返回每个部件的边界框坐标（bounding
box）或分割掩码（segmentation mask）。这一步也可以用轻量本地模型（如
MediaPipe Pose）替代。

**第二步，模板约束裁剪：**根据分割结果，按照上表的插槽规范裁剪各部件。裁剪使用预定义的模板坐标（由
Spine 骨骼的 slot 位置决定）进行对齐，而非直接使用 AI
返回的原始坐标。这保证所有自定义皮肤的部件尺寸和位置与骨骼完全匹配。

**第三步，质量修正：**对裁剪边缘做羽化处理（feathering），避免硬边缘在动画过程中暴露。对小部件（手掌、脚）可用
AI 修复细节。

3.3.4 替代方案：单次 AI 调用直接生成分层图

如果 AI 模型能力足够强（如 GPT-4o 的图像编辑），可以在 Stage 2 和 Stage
3 合并为一次调用：prompt 中直接要求返回分层切片而非全身图。但这对 prompt
的精确控制要求很高，建议作为优化方向而非初始方案。

3.4 Stage 4：Atlas 组装与 Skin 热替换

3.4.1 Atlas 图集组装

将 Stage 3 输出的各部件 PNG 按照预定义的布局模板打包到一张 1024x1024
图集中。这一步在 Rust 侧执行（image crate 的 overlay
操作），同时生成对应的 atlas JSON 文件。

**布局模板的设计原则：**

-   每个部件在图集中的位置和尺寸与预制皮肤的 atlas 完全一致。这样 Spine
    运行时无需解析新的坐标映射，直接替换图集文件即可

-   图集模板从 Spine 项目的预制皮肤 atlas 中提取，确保 region 坐标一致

-   每个部件之间保留 2px 间距（gutter），防止纹理渗透

3.4.2 Skin 热替换流程

Rust 通过 Tauri IPC 将图集数据和 atlas JSON 发送到前端。前端执行：

**Step 1 --- 加载纹理：**将图集 base64 转为 PixiJS Texture 对象。通过
PIXI.Texture.from(base64DataUrl) 创建。

**Step 2 --- 构建 Skin：**通过 Spine 运行时 API 创建新的 Skin
对象，将图集中各 region 挂载到对应 slot 的 attachment 上。伪代码：

const customSkin = new spine.Skin(\'user-custom\');

// 从预制皮肤复制骨骼结构

customSkin.copySkin(skeleton.data.findSkin(\'default\'));

// 替换各插槽的纹理 attachment

for (const \[slotName, region\] of Object.entries(atlasRegions)) {

const slotIndex = skeleton.findSlotIndex(slotName);

const attachment = createRegionAttachment(region, newTexture);

customSkin.setAttachment(slotIndex, slotName, attachment);

}

**Step 3 --- 平滑切换：**调用 skeleton.setSkin(customSkin) 并触发
setSlotsToSetupPose()。可配合 PixiJS 的 alpha 渐变实现 0.3s
的平滑过渡，而非硬切。

4\. Rust 服务层模块设计

4.1 模块结构

src/

skin_gen/

mod.rs // 模块入口，暴露 Tauri commands

preprocess.rs // Stage 1: 图片校验、尺寸归一化

stylize.rs // Stage 2: AI 风格化 API 调用

segment.rs // Stage 3: 部件拆分与裁剪

atlas.rs // Stage 3-\>4: 图集组装

api_client.rs // 统一 AI API 客户端（reqwest）

cache.rs // 本地缓存管理

error.rs // 错误类型定义

4.2 Tauri Commands 接口

  ------------------------ ------------------------ ------------------------
  **Command**              **输入**                 **输出**

  **generate_skin**        photo: base64 String\    atlas_base64: String\
                           style: StyleTemplate     atlas_json: String\
                           enum                     或 SkinGenError

  **get_cached_skins**     无                       Vec\<CachedSkin\>\
                                                    列出所有已缓存皮肤

  **delete_cached_skin**   skin_id: String          bool
  ------------------------ ------------------------ ------------------------

generate_skin 是主命令，内部串联执行 Stage 1→4，通过 Tauri 的 event emit
向前端抨送进度更新（"正在风格化\...""正在拆分部件\..."等）。

4.3 AI API 抽象层

将不同 AI 服务提供商抽象为统一 trait：

#\[async_trait\]

pub trait ImageStyler: Send + Sync {

async fn stylize(&self, image: &\[u8\], prompt: &str)

-\> Result\<Vec\<u8\>, SkinGenError\>;

fn provider_name(&self) -\> &str;

}

初始实现 OpenAiStyler 和 GeminiStyler
两个变体。通过配置文件切换，无需重新编译。API Key 存储在 OS
原生凭据管理器中（复用已有的 keyring 集成）。

4.4 本地缓存策略

生成的皮肤缓存在本地磁盘，避免重复调用 API：

-   缓存位置：{app_data_dir}/skins/{skin_id}/，包含
    atlas.png、atlas.json、thumbnail.png、metadata.json

-   缓存索引：以照片内容 hash + 风格模板为 key，同一照片 +
    同一风格不重复生成

-   容量限制：默认最多缓存 10 套自定义皮肤（约 10MB），LRU
    淘汰最早未使用的

5\. 前端交互设计

5.1 用户流程

整个流程嵌入在设置面板的"角色外观"页签中：

预制皮肤网格

\[猫咪\] \[机器人\] \[狐狸\] \[+ 自定义\]

点击"+ 自定义"后进入三步引导：

-   **Step 1 ---
    上传照片：**拖拽或点击上传，显示裁剪框。裁剪确认后自动进入下一步。

-   **Step 2 --- 选择风格：**提供 2\~3 个风格选项（如"Q
    版萌系""像素复古""精致赛博朋克"），每个选项带预览缩略图。确认后开始生成。

-   **Step 3 ---
    生成中：**显示进度条和阶段提示。桌宠保持当前皮肤正常运行。生成完成后显示预览，用户确认"应用"或"重新生成"。

5.2 状态管理

皮肤生成流程作为一个独立的 React Context 管理，不侵入 XState 状态机：

interface SkinGenState {

status: \'idle\' \| \'uploading\' \| \'stylizing\' \| \'segmenting\'

\| \'assembling\' \| \'ready\' \| \'error\';

progress: number; // 0\~100

preview?: string; // base64 缩略图

atlasData?: AtlasData; // 待应用的图集数据

error?: string;

}

只有当用户点击"应用"时，才触发实际的 Spine Skin
切换。这保证生成过程的任何失败都不会影响当前桌宠状态。

5.3 错误处理与回退

  ----------------- ---------------------------------- ------------------------------
  **失败场景**      **用户感知**                       **系统行为**

  照片无人物        提示"未检测到人物，请重新选择"     不发起 API 调用，返回 Stage 1

  AI API 超时       提示"网络超时，请重试"\            自动重试 1 次，失败后抨送错误
                    + 重试按钮                         

  AI 生成质量差     用户可选"重新生成"                 不强制应用，用户有最终决定权

  图集组装失败      提示"生成失败，请换一张照片试试"   记录错误日志，不影响主流程

  Skin 切换异常     无感知，静默保持当前皮肤           回退到上一个有效皮肤
  ----------------- ---------------------------------- ------------------------------

6\. 关键技术决策与约束

6.1 为什么不用 CartoonAlive / Live2D 路线

-   Live2D 缺乏 PixiJS v8 官方支持，pixi-live2d-display 停留在 v7

-   Live2D Cubism SDK 有独立许可证费用，且商用限制复杂

-   引入 Live2D 意味着替换整个动画管线，已有 30 组 Spine 动画全部废弃

-   CartoonAlive 目前仅为论文阶段，无可集成的 SDK 或稳定 API

6.2 为什么不用 GodMode AI 自动绑骨

-   输入预期是干净角色立绘，不是真人照片，中间质量落差太大

-   每个用户生成的骨骼结构不同，预制动画无法复用

-   自动拆分的身体部件大概率需要大量手动修正

6.3 为什么选择"换皮不换骨"

-   30 组动画× N 套皮肤 = 全量复用，边际成本为零

-   动画调度器、事件队列、优先级系统全部无需改动

-   失败时可无缝回退到预制皮肤，零风险

-   Spine Skin 是引擎原生功能，无额外开发负担

6.4 性能约束

-   图集尺寸统一 1024x1024，与文档 01 规定一致，显存占用约 4MB

-   缓存的 Skin 在切换时从磁盘加载，首次加载 \< 200ms，后续从内存缓存 \<
    10ms

-   AI API 调用是全流程的粒度瓶颈（网络往返），本地计算可忽略不计

7\. 实施计划

7.1 前置条件检查清单

  ------------------------------------- -------------------- ------------
  **条件**                              **验证方法**         **状态**

  Spine Skin 手动切换已跑通（≥2         在应用中切换验证     待完成
  套预制皮肤）                                               

  Spine 图集模板规格确定（slot          导出 atlas JSON 对比 待完成
  布局、region 坐标）                                        

  动画调度器支持 Skin                   运行时切换测试       待完成
  热切换（不打断当前动画）                                   

  AI API Key 配置与存储机制已就绪       keyring 集成测试     待完成
  ------------------------------------- -------------------- ------------

7.2 开发步骤（4 个 Sprint）

**Sprint 1（第 1 周）--- 端到端骨架：**

-   前端：照片上传组件 + 裁剪框 + 风格选择界面（可用 mock 数据）

-   Rust：preprocess.rs + api_client.rs 骨架，先跟一个 AI API
    跑通风格化调用

-   里程碑：上传照片→获得卡通风格图，显示在前端预览中

**Sprint 2（第 2 周）--- 部件拆分与图集组装：**

-   Rust：segment.rs 实现部件拆分（AI API 或本地模型）+ atlas.rs
    图集打包

-   制作 atlas 布局模板（从预制皮肤的 Spine 导出中提取坐标）

-   里程碑：从卡通图生成可用的 atlas.png + atlas.json

**Sprint 3（第 3 周）--- Skin 热替换集成：**

-   前端：实现 atlas 加载 → Skin 构建 → setSkin() 热替换全流程

-   Rust：cache.rs 实现本地缓存 + get_cached_skins 命令

-   进度事件推送：生成过程中 Tauri event 实时更新前端状态

-   里程碑：上传照片→桌宠变成新皮肤，动画正常播放

**Sprint 4（第 4 周）--- 打磨与健壮性：**

-   错误处理全路径覆盖（无人物检测、API 失败、图集异常）

-   Prompt 调优（收集实际用户照片测试，迭代风格化效果）

-   缓存管理界面（查看/删除已生成皮肤）

-   平滑过渡动画（旧皮肤 fadeOut → 新皮肤 fadeIn）

-   里程碑：功能可发布，各种边界场景不崩溃

7.3 成本估算

  ----------------------------- ----------------------- ------------------------
  **项目**                      **单次生成成本**        **备注**

  GPT-4o image edit             \~\$0.02--0.08          取决于图片尺寸和复杂度

  Gemini 2.0 Flash              \~\$0.005--0.02         成本最低的云端选项

  自托管 SD + LoRA              \~\$0.001--0.005        需要 GPU 服务器固定成本

  本地计算（图片处理、Atlas）   免费                    CPU 即可，无 GPU 要求
  ----------------------------- ----------------------- ------------------------

假设用户平均生成 3 次才满意，单用户成本约 \$0.06--0.25（GPT-4o）或
\$0.015--0.06（Gemini）。

8\. 未来扩展方向

-   **多角度生成：**当前方案基于单角度立绘。未来可拓展为多角度参考图生成，提升纹理在侧视动画时的表现。

-   **风格 LoRA 训练：**收集足够多预制皮肤的美术资产后，微调一个专用
    LoRA，替代通用 AI API，实现风格更稳定、成本更低。

-   **社区皮肤市场：**用户生成的皮肤可分享到社区市场，他人可直接下载使用（已经是标准
    atlas 格式）。

-   **CartoonAlive 集成：**当该项目发布可用 SDK
    后，可作为"高级"皮肤生成方案接入，生成带表情动画的 Live2D
    面部层，叠加在 Spine 身体之上。
