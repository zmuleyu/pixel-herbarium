# check-collab

Run:

```powershell
powershell -File D:\tools\scripts\collab-check.ps1 -ProjectPath D:\projects\Games\pixel-herbarium
```

If the check exposes a real process issue, record it in `collab/ops/collab-issues.md`.
