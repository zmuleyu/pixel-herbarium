import { View } from 'react-native';

// Root index — renders nothing.
// Navigation is handled by _layout.tsx redirect logic (auth state + onboarding check).
export default function Index() {
  return <View />;
}
